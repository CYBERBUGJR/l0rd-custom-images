# NetBox plugins for l0rd.be. Versions checked against NetBox 4.6.7 (chart appVersion).
# Bump BASE in the Dockerfile together with the chart dep in gitops_l0rd.be.
netbox-reorder-rack==1.1.4
netbox-plugin-dns==1.5.11
netbox-bgp==0.19.0
# 4.5.1 is the latest release and targets NetBox 4.5; no 4.6 release exists yet.
# It declares no min/max_version, so NetBox loads it. If it breaks startup, drop
# netbox_topology_views from the `plugins` list in values.yaml (no rebuild needed).
netbox-topology-views==4.5.1
